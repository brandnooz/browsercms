<%= content_for :html_head do %>
  <style type="text/css">
    div.field {
      margin: 10px 0;
    }
    div.field div.label {
      display: inline;
      font-weight: bold;
    }
    div.field div.value {
      display: inline;
    }
  </style>
<% end %>

<%= content_for :functions do %>
  <div class="btn-toolbar">
    <%= icon_link_to("List All", cms_email_messages_path, 'list', :class => "btn") %>
  </div>
<% end %>

<% @page_title = @toolbar_title = "Email Message '#{@message.subject}'" %>
<div class="field">
  <div class="label">Delivered At:</div> 
  <div class="value"><%= time_on_date(@message.delivered_at) %></div>
</div>
<% unless @message.content_type.blank? %>
  <div class="field">
    <div class="label">Content-Type:</div> 
    <div class="value"><%= @message.content_type %></div>  
  </div>
<% end %>
<div class="field">
  <div class="label">From:</div> 
  <div class="value"><%= @message.sender %></div>
</div>
<div class="field">
  <div class="label">To:</div> 
  <div class="value"><%= @message.recipients %></div>
</div>
<% unless @message.cc.blank? %>
  <div class="field">
    <div class="label">CC:</div> 
    <div class="value"><%= @message.cc %></div>
  </div>
<% end %>
<% unless @message.bcc.blank? %>
  <div class="field">
    <div class="label">BCC:</div> 
    <div class="value"><%= @message.bcc %></div>
  </div>
<% end %>
<div class="field">
  <div class="label">Subject:</div> 
  <div class="value"><%= @message.subject %></div>
</div>
<div class="field">
  <%= @message.body %>
</div>
